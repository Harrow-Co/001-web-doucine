[build]
builder = "nixpacks"
buildCommand = "npm install && cd server && tsc"

[deploy]
startCommand = "node server/dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
EVENT_SERVER_PORT = "3000"
NODE_ENV = "production"
